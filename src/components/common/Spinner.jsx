import React from 'react';

// Simple CSS spinner to replace BeatLoader
const Spinner = () => (
  <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-white"></div>
);

export default Spinner;